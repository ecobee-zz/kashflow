/* $Id:$ */

A utility module providing initial integration into the Kashflow API. 

You probably do not need to install this module unless another module you are 
using requires it. It provides no new features to your drupal site. It only 
provides an API other modules can leverage.

